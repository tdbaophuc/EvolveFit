{
  "name": "@evolvefit/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "tsx src/server.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@evolvefit/shared": "file:../../packages/shared",
    "@fastify/cookie": "^11.0.2",
    "@fastify/cors": "^11.0.1",
    "@fastify/rate-limit": "^10.3.0",
    "fastify": "^5.4.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "tsx": "^4.20.3"
  }
}
